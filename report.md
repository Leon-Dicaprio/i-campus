# 项目阶段报告：校园智能客服系统
## 一、项目执行进展情况
截至目前，项目整体已完成核心功能架构搭建，并在多个关键技术模块上取得阶段性成果。系统采用 Go (Golang) 作为主要开发语言，确保了服务的高并发性能与可维护性，基本达到既定阶段目标。

### 1. 后端基础能力建设
后端系统基于 Go 标准库 net/http 构建，采用 RESTful API 架构，保证了服务的轻量化与高效性。

- 用户与会话管理 ：基于 golang.org/x/crypto 实现了安全的用户认证体系，支持用户注册、登录及身份校验。
- 上下文管理 ：构建了基于内存的会话历史管理器（ HistoryManager ），支持多轮对话上下文的持久化与维护，为后续智能问答提供了稳定的状态管理基础。
- 架构设计 ：采用模块化分层设计（Controller-Service-Repository），充分考虑了后续功能扩展与高并发场景下的系统可演进性。
### 2. Hybrid RAG 知识库构建
项目已初步完成 Hybrid RAG（混合检索增强生成） 知识库的构建，结合了本地向量检索与语义理解能力。

- 向量数据库 ：部署 Milvus 作为核心向量存储引擎，配置了 2048 维向量索引（L2 距离度量），确保毫秒级的检索响应速度。
- 数据处理流水线 ：开发了基于 ledongthuc/pdf 的文档解析脚本，能够自动抓取并清洗学校官网及学生手册等非结构化数据，完成结构化处理。
- 嵌入模型 ：集成 智谱 AI (Embedding-3) 模型进行高质量文本向量化，有效提升了系统对校内规章制度、事务流程等问题的回答准确性与一致性。
### 3. 联网搜索能力引入
针对知识库难以覆盖的时效性问题与动态信息查询需求，系统进一步引入了实时联网搜索模块。

- 搜索引擎集成 ：实现了自定义的 Bing 搜索采集器 ，并针对校园域（ site:dgut.edu.cn ）进行了专项优化。
- 动态决策机制 ：基于 golang.org/x/net/html 实现搜索结果页面的实时解析。当 Decision Agent（决策智能体） 判断本地知识库置信度不足时，系统会自动切换至联网搜索流程，并对外部检索结果进行摘要与整合，显著提升了问答的覆盖范围与实用性。
### 4. MCP Server 能力集成
项目已完成 MCP (Model Context Protocol) 协议的初步集成，实现了外部工具能力的统一管理与调用。

- 协议实现 ：实现了标准的 JSON-RPC over HTTP/SSE 客户端，用于与 MCP Server 进行实时通信。
- 场景落地 ：成功接入 高德地图 (AMap) 服务。系统能够根据大模型的语义判断，按需调用地图能力进行地点查询与周边设施搜索，实现了具备“行动能力”的智能客服场景。
### 5. Prompt 调优与多场景适配
设计了基于 GLM-4 大模型的 Decision Agent 模块，实现了用户意图的精准路由。

- 意图识别 ：通过精细化的 Prompt 工程，模型能够准确将用户问题分类为“本地知识库”（规章）、“联网搜索”（通知）或“MCP 工具”（导航/地图）。
- 输出优化 ：针对不同场景约束了模型的输出风格与信息来源，有效减少了幻觉现象，提升了回答的准确性与可解释性。
### 6. 当前存在的问题
尽管系统已实现主要功能，但在性能与工程化层面仍存在一定不足：

- 检索性能 ：大规模数据场景下，Milvus 的检索逻辑与 PDF 解析脚本的执行效率仍有优化空间。
- 结果融合 ：多渠道（本地向量 + 联网搜索）检索结果的重排序（Re-ranking）与融合策略需进一步精细化。
- MCP 延迟 ：在高频调用场景下，基于 SSE 的 MCP 客户端通信延迟与资源消耗仍需改进。
## 二、项目后期工作计划
在后续阶段，项目将重点围绕系统性能优化、工程化升级及应用场景拓展展开：

### 1. Hybrid RAG 优化
- 性能调优 ：优化 Milvus 客户端与 OpenAI SDK 的连接池管理，降低 I/O 开销。
- 渠道扩展 ：扩展检索渠道，支持更多格式的校内文档数据源。
- 召回策略 ：引入重排序模型，优化向量召回结果，提升最终答案的相关度与准确率。
### 2. MCP Server 能力增强
- 通信优化 ：优化 SSE 客户端的消息处理机制，降低握手与传输延迟。
- 资源管理 ：实现更智能的工具加载策略，减少不必要的 Token 消耗，实现按需调用。
- 稳定性 ：增加对外部 MCP 服务调用的熔断与重试机制，提升系统鲁棒性。
### 3. 系统架构升级
- 微服务化 ：逐步推进系统微服务化改造，将检索（RAG）、搜索、MCP 等模块解耦为独立服务。
- 并发支持 ：优化 Go 协程调度模型，支持更高并发访问场景，提升系统整体稳定性。
### 4. 第三方系统接入
- 教务系统 ：计划通过 API 或 MCP 方式接入教务系统，支持查课表、查成绩等个性化服务。
- 生态对接 ：探索与更多校园信息系统的对接方式，进一步丰富智能客服的服务能力与应用场景。